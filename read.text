*** para usar impor se modifica el package.j y se agrega arriba de autor "type": "module"
*** se intala pug para los template  con npm install pug

**se instala una dependecia para conectar ala base de datos 
    npm install mysql2 sequelize





*** para hacer un deploimen  para subir los archivos a un servidos 

se intala  la dependecina para generar variables de entorno
npm install --save-dev dotenv
se crea un archivo  como variables.env